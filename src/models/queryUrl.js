import CONFIG from './m-config.js';

const QueryUrls = {

    caht : CONFIG.backendUrl + 'chat',
    login : CONFIG.backendUrl + 'login',
    register : CONFIG.backendUrl + 'register',

};

export { QueryUrls };
